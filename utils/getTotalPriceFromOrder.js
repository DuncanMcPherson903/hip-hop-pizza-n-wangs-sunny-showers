const getTotalPriceFromOrder = () => {

};

export default getTotalPriceFromOrder;

// ${item.status ? `<button class="btn btn-link text-danger p-0 text-decoration-none" id="close-order-btn--${item.firebaseKey}">Close</button>` : ''}
